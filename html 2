<!DOCTYPE html>
<html>
<head>
    <title>Thu Thu Account</title>
    <style>
        body { font-family: Arial; display: flex; justify-content: center; background: #1a1a1a; color: white; }
        .shop-card { background: #2a2a2a; padding: 30px; border-radius: 15px; text-align: center; margin-top: 50px; width: 320px; border: 1px solid #444; }
        .price { font-size: 32px; color: #00d1b2; margin: 15px 0; }
        #qr-image { width: 200px; background: white; padding: 10px; border-radius: 10px; display: none; margin: 0 auto; }
        button { background: #00d1b2; border: none; padding: 12px 25px; color: white; border-radius: 5px; cursor: pointer; font-weight: bold; }
    </style>
</head>
<body>
    <div class="shop-card">
        <h1>Thu Thu Account</h1>
        <p>Premium Game Login</p>
        <div class="price">$10.00</div>
        <button onclick="getPayment()">Buy Now with KHQR</button>
        <br><br>
        <img id="qr-image" src="" alt="Payment QR">
        <p id="status"></p>
    </div>

    <script>
        async function getPayment() {
            const res = await fetch('/generate-qr');
            const data = await res.json();
            document.getElementById('qr-image').src = data.qrCode;
            document.getElementById('qr-image').style.display = 'block';
            document.getElementById('status').innerText = "Scan to Pay via KHQR";
        }
    </script>
</body>
</html>
